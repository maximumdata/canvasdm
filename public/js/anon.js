console.log(`this should be the javascript file that a tablet can run to allow access to any player to move any other player.

    the dm will need this functionality, so maybe develop that first and then use a subset of the code to only apply to player characters and put it here.

    also gonna need a menu to select which character you want to move`);

let socket = io.connect();
